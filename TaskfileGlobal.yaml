version: 3

vars:
  VERSION: 0.1.0

includes:
  aws: "~/.task/aws/Taskfile.yaml"

tasks:
  update:
    dir: ~/.task
    cmds:
    - git pull
    - cp ~/.task/TaskfileGlobal.yaml ~/Taskfile.yaml

  version: echo {{ .VERSION }}