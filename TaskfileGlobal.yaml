version: 3

vars:
  VERSION: 0.2.0

includes:
  aws: "~/.task/aws/Taskfile.yaml"
  test: "~/.task/test/Taskfile.yaml"
  cloudflare: "~/.task/cloudflare/Taskfile.yaml"

tasks:
  update:
    dir: ~/.task
    cmds:
    - git pull
    - cp ~/.task/TaskfileGlobal.yaml ~/Taskfile.yaml

  version: echo {{ .VERSION }}